(()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const money=n=>Math.round(n).toLocaleString('cs-CZ')+' Kč';function type(){return $('.type.on')?.dataset.id||''}function num(s){const m=String(s||'').replace(/\s/g,'').match(/([0-9][0-9.]*)Kč/);return m?+(m[1].replace(/\./g,'')):0}function rows(){return $$('.resultbody .row').filter(r=>!r.classList.contains('off'))}function label(r){return (r.querySelector('span')?.childNodes?.[0]?.textContent||r.querySelector('span')?.textContent||'').trim().toLowerCase()}function render(){if(type()!=='mobile'||window.PLOTAO_EXTRA?.variant!=='mesh')return;const box=$('#extraPriceBox');if(!box||getComputedStyle(box).display==='none')return;const strong=[...box.querySelectorAll('strong,b')].reverse().find(x=>/Kč/.test(x.textContent||'')&&!/[–-]/.test(x.textContent||''));const material=num(strong?.textContent);if(!material)return;let extras=0,fill=null;for(const r of rows()){const l=label(r);if(l.includes('plotová výplň')||l.includes('plotova vypln'))fill=r;else extras+=num((r.querySelector('b,strong')||r.lastElementChild)?.textContent)}if(!fill)return;const target=fill.querySelector('b,strong')||fill.lastElementChild;if(target)target.textContent=money(material);const total=material+extras;if($('.price strong'))$('.price strong').textContent=money(total);if($('.mobile-price strong'))$('.mobile-price strong').textContent=money(total)}document.addEventListener('plotao:extra',()=>setTimeout(render,50));document.addEventListener('input',()=>setTimeout(render,80));document.addEventListener('change',()=>setTimeout(render,80));document.addEventListener('click',()=>setTimeout(render,100));setInterval(render,1000);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render);else render();})();